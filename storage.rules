rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Users can read/write only their own pint photos
    match /pints/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
